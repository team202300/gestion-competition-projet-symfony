{% extends 'basedash.html.twig' %}

{% block title %}TypeCompetitions{% endblock %}

{% block body %}
    <h1>TypeCompetitions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_competition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_competition.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_competitions_index') }}">back to list</a>

    <a href="{{ path('app_type_competitions_edit', {'id': type_competition.id}) }}">edit</a>

    {{ include('type_competitions/_delete_form.html.twig') }}
{% endblock %}
