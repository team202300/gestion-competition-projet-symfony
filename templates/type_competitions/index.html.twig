{% extends 'basedash.html.twig' %}

{% block title %}TypeCompetitions index{% endblock %}

{% block body %}
    <h1>TypeCompetitions index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_competition in type_competitions %}
            <tr>
                <td>{{ type_competition.id }}</td>
                <td>{{ type_competition.name }}</td>
                <td>
                    <a href="{{ path('app_type_competitions_show', {'id': type_competition.id}) }}">show</a>
                    <a href="{{ path('app_type_competitions_edit', {'id': type_competition.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_competitions_new') }}">Create new</a>
{% endblock %}
